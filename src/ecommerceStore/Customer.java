package ecommerceStore;

public class Customer extends User{
    public Customer(String name, int i, String mail, String address, String password, String number) {
        super (name, i, mail, address, password, number );
    }
}
